global_params:
  seed: 0

columns:
  - column_name: "hi"
    column_type: "type"
    data_type: str
    encoder:
      - name: OneHotEncoder
        params:
          alphabet: 2 # Wrong
  
  - column_name: "Guten Tag"
    column_type: "type"
    data_type: 35.6 # Wrong
    encoder:
      - name: 9 # Wrong
        params:


transforms:
  - transformation_name: "D"
    columns:
      - column_name: "col2"
        transformations:
          - name: 4 # Wrong
            params:
              probability: "error" # Wrong

split:
  - split_method: 3 # Wrong
    params:
      split: [[0.6, 0.2, 0.2]]